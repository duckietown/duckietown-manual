(lx-testing)=
# Testing Your LX

Provide unit tests for students in the `tests` directory. They can be run in VSCode using the `Testing` plugin.

```{image} ../../_images/lx-devmanual/consume/test-interface.png
```

```{note}
If you would like to see dedicated `dts` commands for testing, let us know on Slack!  
```
