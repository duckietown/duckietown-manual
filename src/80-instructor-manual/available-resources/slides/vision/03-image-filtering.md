(slides-and-recordings-image-filtering)=
# Image Filtering

```{seo}
:description: Image filtering in Duckietown
:keywords: image filtering, convolution, image processing, image gradients, Canny edge detector
```

We can modify an image by designing a filter and convolving it with the original image. The way we design this filter will depend on how we would like to process the image. In this video, we describe this filtering process and describe a few import types of filters, such as ones that would be useful to help us detect lines or corners in an image. 

```{vimeo} 549643768
:alt: Image Filtering
```

## Image Gradients

These slides cover the notion of image gradients:

```{slides} ../../../../_assets/instructor-manual/image-gradients.pdf
```

